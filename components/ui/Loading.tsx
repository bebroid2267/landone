const Loading = () => (
  <div className="fixed z-[99999] inset-0 flex items-center justify-center bg-white">
    <span className="text-6xl md:text-8xl animate-pulse select-none">🐾</span>
  </div>
)

export default Loading
